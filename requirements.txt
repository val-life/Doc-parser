# Install the CUDA-enabled torch build from https://pytorch.org/get-started/locally/

# ──────────────────────────────────────────────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cu126

# Core AI/ML
torch>=2.10.0
torchvision>=0.16.0
transformers>=4.50.0
accelerate>=0.30.0
sentencepiece>=0.2.0
tiktoken>=0.7.0

# Document Processing
pymupdf>=1.24.0
docx2pdf>=0.1.8
Pillow>=10.0.0
trafilatura>=1.10.0

# API & Networking
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
requests>=2.31.0

# ──────────────────────────────────────────────────────────────────────────────
# Optional: flash attention (Ampere+ GPUs only, Linux recommended)
# Uncomment if you want to use flash attention with LightOnOCR-2-1B.
# ──────────────────────────────────────────────────────────────────────────────
# flash-attn>=2.5.0
